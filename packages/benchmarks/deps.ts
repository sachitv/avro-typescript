/**
 * Benchmark-specific dependencies
 *
 * This file centralizes all benchmark dependencies to keep them separate
 * from the main package dependencies.
 */

// Currently using Deno.bench (built-in), no external dependencies needed